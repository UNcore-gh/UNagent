# Public repo is binaries-only. Source lives in the private UNagent-src repo.

# runtime data (may contain API keys — never commit)
data.json

# dependencies
node_modules/

# system
.DS_Store

# source & dev files — removed from the public repo, kept in UNagent-src
/src/
package.json
package-lock.json
tsconfig.json
esbuild.config.mjs
import-meta-url-shim.js
jest.config.js
jest.setup.js
version-bump.mjs
versions.json

# build junk (main.js IS tracked — it's the shipped binary)
meta.json
*.js.map